const outsideClick=(event,notelem)=>{let i;let clickedOut=true;const len=notelem.length;for(i=0;i<len;i+=1){if(event.target===notelem[i]||notelem[i].contains(event.target)){clickedOut=false;}}
if(clickedOut)return true;return false;};const open_menu=(element)=>{const menu_nav=document.querySelector('.menu-nav');menu_nav.classList.toggle('open');const element_child=Array.from(element.children);element_child.forEach((el)=>{if(el.classList.contains('icon-menu')){el.classList.remove('icon-menu');el.classList.add('icon-close');}else{el.classList.remove('icon-close');el.classList.add('icon-menu');}});element.classList.toggle('active');};const field=document.querySelector('.is-search');const search=()=>{const button=document.getElementById('search');if(field.classList.contains('is-show')){field.classList.remove('is-show');button.classList.remove('is-close');return;}
field.classList.add('is-show');button.classList.add('is-close');};